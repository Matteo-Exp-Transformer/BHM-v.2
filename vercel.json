{
  "buildCommand": "npm install --include=optional && npm run build",
  "installCommand": "npm install --include=optional",
  "framework": "vite",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
